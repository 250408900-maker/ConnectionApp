# Copy to .env.local for local overrides (Expo picks up EXPO_PUBLIC_*).
EXPO_PUBLIC_SERVER_URL=http://130.61.28.42:3000

# Server (export these in the process that runs node index.js)
# PORT=3000
# CORS_ORIGIN=*
